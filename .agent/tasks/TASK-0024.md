---
template: task-record
template_version: 2.0.0
template_digest: "a658102714db39f1bfeb3bb4fde325df53820feb73c12d5b7a26b47b9ec105ac"
project_owned: true
record_contract: task-record
record_schema_version: 1
id: TASK-0024
status: completed
milestone_id: MILESTONE-0013
---

# Task: Restore PPW-standard pre-commit hook installation

## Objective

Remove the custom `.githooks` / `core.hooksPath` mechanism introduced by TASK-0022 and restore PPW-style developer bootstrap using pre-commit's own standard Git hook installer.

## Specification

- Follow PPW's post-generation behavior: install development dependencies, then run `pre-commit install --install-hooks`.
- Do not commit a Git hook or configure `core.hooksPath` as the normal Automo workflow.
- Migrate only the known legacy Automo `core.hooksPath=.githooks` value from affected clones.
- Verify that the resulting Git hook path contains an executable `pre-commit` hook before bootstrap reports success.
- Keep the canonical full-repository pre-commit/tox gate unchanged.

## Ordered plan

1. Re-check PPW's `hooks/post_gen_project.py` bootstrap behavior.
2. Remove `.githooks/pre-commit` and `core.hooksPath` setup.
3. Run pre-commit's standard installer from the Poetry environment.
4. Add executable-hook and legacy-migration regression tests.
5. Update current contributor/GetDone records without rewriting prior completed-task history.
6. Run focused/full tests, source/compile checks, and GetDone validation.

## Acceptance criteria

- [x] AC-001: `.githooks/` is not part of the current source tree.
- [x] AC-002: bootstrap runs `poetry run pre-commit install --install-hooks`.
- [x] AC-003: bootstrap does not configure a custom `core.hooksPath` during normal setup.
- [x] AC-004: legacy `core.hooksPath=.githooks` is removed for affected clones.
- [x] AC-005: bootstrap verifies the installed Git hook is executable.
- [x] AC-006: current developer documentation describes `.git/hooks/pre-commit`, not `.githooks`.

## Validation evidence

- Focused bootstrap/release tests pass.
- Full pytest, source hygiene, compile checks, and GetDone validation pass.

## Outcome

Completed. Automo now follows PPW's actual pre-commit installation pattern and no longer relies on a committed non-executable hook file.

## Remaining work

- Connected canonical gate/package evidence remains the release blocker under MILESTONE-0013.
