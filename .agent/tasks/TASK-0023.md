---
template: task-record
template_version: 2.0.0
template_digest: "a658102714db39f1bfeb3bb4fde325df53820feb73c12d5b7a26b47b9ec105ac"
project_owned: true
record_contract: task-record
record_schema_version: 1
id: TASK-0023
status: completed
milestone_id: MILESTONE-0013
---

# Task: Repair pre-commit YAML validity and protect bootstrap configuration

## Objective

Fix the invalid pre-commit YAML introduced by TASK-0022 and add a regression test that parses the complete configuration before bootstrap/release handoff.

## Specification

- Quote YAML scalar values that contain mapping-significant punctuation.
- Parse `.pre-commit-config.yaml` in the release-contract test rather than validating only text fragments.
- Keep the unified local/GitHub/tox quality-gate design unchanged.

## Ordered plan

1. Reproduce the YAML parser error at the reported line.
2. Correct the invalid hook name scalar.
3. Add structural YAML parsing to release-contract tests.
4. Run focused and full tests plus source/compile checks.
5. Preserve NEXT-0022 as the connected release-evidence step.

## Acceptance criteria

- [x] AC-001: `.pre-commit-config.yaml` parses as YAML.
- [x] AC-002: The tox hook remains configured with `poetry run tox`, `pass_filenames: false`, and `always_run: true`.
- [x] AC-003: Release-contract tests fail on syntactically invalid pre-commit YAML.
- [x] AC-004: Full local pytest/source/compile validation passes.

## Validation evidence

- PyYAML parse -> 4 repositories loaded.
- Focused release/bootstrap tests -> 15 passed.
- Full pytest -> 129 passed.
- `python scripts/source_check.py` -> pass.
- `PYTHONPATH=src python -m compileall -q src scripts tests examples` -> pass.

## Outcome

Completed. Bootstrap can now parse and provision the canonical pre-commit configuration, and syntax validity is regression-tested.

## Remaining work

- Connected execution evidence remains NEXT-0022 / M13 EC-010 through EC-013.
