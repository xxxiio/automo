---
template: task
template_version: 2.0.0
project_owned: true
record_contract: task
record_schema_version: 1
id: TASK-0031
status: completed
milestone_id: MILESTONE-0013
---

# Task: Repair pytest pre-commit hook contract

## Objective

Repair the tox-free pre-commit pytest hook so the executable YAML configuration exactly matches the release contract and cannot regress through formatting-only assertions.

## Scope

- Ensure `.pre-commit-config.yaml` runs `poetry run pytest -q`.
- Ensure the pytest hook is `language: system`, `pass_filenames: false`, and `always_run: true`.
- Parse the YAML in release-contract tests and assert the actual hook object rather than raw formatting text.
- Remove stale active evidence that still describes superseded tox/interpreter-validation behaviour.

## Acceptance criteria

- [x] AC-001: `.pre-commit-config.yaml` parses successfully.
- [x] AC-002: pytest pre-commit hook entry is exactly `poetry run pytest -q`.
- [x] AC-003: pytest hook is always-run and receives no filenames.
- [x] AC-004: focused public-release tests pass.
- [x] AC-005: full repository test suite and source checks pass.
