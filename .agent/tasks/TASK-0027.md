---
template: task-record
template_version: 2.0.0
template_digest: "a658102714db39f1bfeb3bb4fde325df53820feb73c12d5b7a26b47b9ec105ac"
project_owned: true
record_contract: task-record
record_schema_version: 1
id: TASK-0027
status: completed
milestone_id: MILESTONE-0013
---

# Task: Guard release source completeness

## Objective

Prevent release/source handoffs from silently omitting hidden repository files that are required by pre-commit, CI, release-health tests, and packaging validation.

## Specification

- Treat `.gitignore`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`, `.github/workflows/publish-pypi.yml`, `pyproject.toml`, `poetry.lock`, and `tox.ini` as required release-source paths.
- Make `scripts/source_check.py` fail clearly when any required path is absent.
- Preserve TASK-0026's PPW-style tox behavior: local pre-commit runs `tox -e py`; plain `tox` remains the explicit full tox env-list command.

## Acceptance criteria

- [x] AC-001: source-check rejects a candidate missing a required hidden release file.
- [x] AC-002: the complete source tree passes source-check and the unit suite.
- [x] AC-003: the handoff archive contains hidden files/directories.
- [x] AC-004: no Python 3.13 local requirement is introduced.

## Outcome

Completed. Release-source completeness is now checked explicitly, and the corrected handoff archive preserves dotfiles.
