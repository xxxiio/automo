---
template: task-record
template_version: 2.0.0
template_digest: "a658102714db39f1bfeb3bb4fde325df53820feb73c12d5b7a26b47b9ec105ac"
project_owned: true
record_contract: task-record
record_schema_version: 1
id: TASK-0025
status: completed
milestone_id: MILESTONE-0013
---

# Task: Fix tox requirement parsing in canonical pre-commit gate

## Objective

Repair the tox configuration so the canonical pre-commit unit-test hook can start tox successfully on developer machines and GitHub.

## Specification

- Keep tox 4 as the supported test orchestrator.
- Avoid INI list syntax that splits a version range into an invalid bare requirement.
- Keep the Python 3.11, 3.12, and 3.13 test matrix unchanged.
- Add regression coverage for the exact tox requirement spelling.
- Do not change the unified pre-commit/tox gate design.

## Ordered plan

1. Inspect the failing tox parser input from the developer run.
2. Replace the ambiguous `tox>=4.30,<5` INI value with an unambiguous tox 4 lower-bound requirement.
3. Add release-contract regression assertions.
4. Run focused/full tests, source/compile checks, and GetDone validation.

## Acceptance criteria

- [x] AC-001: tox no longer receives a bare `<5` requirement from `tox.ini`.
- [x] AC-002: `tox.ini` requires `tox>=4.30` and retains py311/py312/py313.
- [x] AC-003: release-contract tests reject the broken `tox>=4.30,<5` spelling.
- [x] AC-004: no canonical workflow semantics change beyond tox-config parsing.

## Validation evidence

- Focused release/bootstrap tests pass.
- Full pytest, source hygiene, compile checks, and GetDone validation pass.

## Outcome

Completed. The tox pre-commit hook now has a parseable tox 4 provisioning requirement.

## Remaining work

- Connected canonical gate/package evidence remains the release blocker under MILESTONE-0013.
