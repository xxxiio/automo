---
template: task-completion-report
template_version: 2.0.0
template_digest: "db6324a99de1ba27b9820a442cf545a98670b3e0d104c244c3325ccf19aae899"
project_owned: true
record_contract: task-completion-report
record_schema_version: 1
status: final
task_id: TASK-0022
---

# Task Completion Report

## Outcome

Completed locally. The staged-file/local versus all-files/CI mismatch is removed: both paths now invoke the same full-repository pre-commit gate, and tox owns Python 3.11–3.13 unit tests within that gate.

## Changes made

- Added `tox.ini` with `py311`, `py312`, and `py313`.
- Added `tox` to Poetry dev dependencies.
- Added a filename-independent tox pre-commit hook.
- Added `.githooks/pre-commit` to run the full-repository gate.
- Updated developer bootstrap to configure `core.hooksPath=.githooks` and provision hook environments.
- Reworked `.github/workflows/ci.yml` so the quality job installs all three Python versions and runs the identical pre-commit command.
- Removed the redundant GitHub pytest matrix.
- Updated public/developer/GetDone documentation and regression tests.

## Behaviour added or corrected

- AC-001/AC-002: local and remote gate command/scope are identical.
- AC-003/AC-004: tox owns supported-version unit tests inside pre-commit.
- AC-005: Poetry remains canonical; uv is not part of developer or GitHub workflows.
- AC-006: release-contract tests assert these properties.

## Tests added or changed

- Updated `tests/test_init_dev.py`.
- Updated `tests/test_public_release.py`, including tox and full-gate assertions.

## Validation results

- `pytest -q` -> pass, 129 tests.
- `python -m compileall -q src tests scripts` -> pass.
- `python scripts/source_check.py` -> pass.
- Full tox/pre-commit execution was not run in this network-constrained container because hook/tool environments are unavailable; connected CI remains authoritative release evidence.

## Documentation updated

- `README.md`
- `CONTRIBUTING.md`
- `docs/PROJECT-SKELETON.md`
- `docs/publishing.md`
- `docs/release-contract.md`
- active GetDone command/status/roadmap/next-step records

## Design decisions

- Git commit intentionally runs the whole repository, accepting a slower commit in exchange for exact local/CI parity.
- Tox is the only supported-version test orchestrator; GitHub does not duplicate pytest matrices.
- Packaging/smoke and docs remain outside pre-commit.
- Ruff `E501` is ignored so the formatter owns line wrapping; `_root_option` is explicitly treated as an immutable B008 default because it intentionally captures process-start CWD.

## Known limitations

- Developers need Python 3.11, 3.12, and 3.13 installed for the full commit gate; tox fails if any are missing.

## Remaining TODOs

- None for TASK-0022.

## Risks and blockers

- M13 still requires connected pre-commit/tox and Poetry artifact evidence.

## Acceptance gate results

| Gate ID | Status | Evidence |
|---|---|---|
| AC-001 | pass | `.githooks/pre-commit` |
| AC-002 | pass | `.github/workflows/ci.yml` |
| AC-003 | pass | `tox.ini` |
| AC-004 | pass | `.pre-commit-config.yaml`; no CI pytest matrix |
| AC-005 | pass | contributor/public workflow docs |
| AC-006 | pass | 129 local tests |

## Next deterministic step

NEXT-0022 — run connected canonical pre-commit/tox plus Poetry Core package/smoke release gates and record EC-010 through EC-013 evidence.
