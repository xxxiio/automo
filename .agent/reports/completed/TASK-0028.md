---
template: task-completion-report
template_version: 2.0.0
template_digest: "db6324a99de1ba27b9820a442cf545a98670b3e0d104c244c3325ccf19aae899"
project_owned: true
record_contract: task-completion-report
record_schema_version: 1
status: final
task_id: TASK-0028
---

# Task Completion Report

## Outcome

Completed. Developer bootstrap is reduced to PPW's current-Python -> pre-commit -> Poetry -> project-install model.

## Changes made

- Changed bootstrap to install `pre-commit` using `sys.executable -m pip`.
- Changed bootstrap to run standard `pre-commit install` directly, matching PPW.
- Changed bootstrap to install Poetry using the same current Python, then run `poetry install --with dev`.
- Removed the requirement that Poetry already be installed before bootstrap.
- Kept the narrow migration for the obsolete Automo `.githooks` setting so affected clones self-heal.
- Updated contributor/public/project-skeleton guidance to remove manual `.venv` and Conda-specific setup assumptions.
- Clarified standard commit scope versus the explicit/GitHub `--all-files` gate.

## Behaviour added or corrected

- Conda, an already-active venv, or plain Python can all be the bootstrap Python.
- Poetry owns the project environment; developers do not manually stack or create `.venv` as part of Automo setup.
- Standard `git commit` uses the standard pre-commit hook; the tox hook is always-run and tests the Poetry-selected active Python.
- The explicit local full gate and GitHub quality job both use `pre-commit run --all-files`.
- GitHub remains responsible for the Python 3.11/3.12/3.13 compatibility matrix.

## Tests added or changed

- Bootstrap tests pin PPW tool-install order and standard hook installation.
- Release-contract tests pin the simplified PPW bootstrap and active-Python tox behavior.

## Acceptance gate results

| Gate ID | Status | Evidence |
|---|---|---|
| AC-001 | pass | bootstrap implementation + tests |
| AC-002 | pass | bootstrap implementation + tests |
| AC-003 | pass | README/CONTRIBUTING/project skeleton |
| AC-004 | pass | `.pre-commit-config.yaml` |
| AC-005 | pass | `tox.ini` + `.github/workflows/ci.yml` |
| AC-006 | pass | 132 tests; source-check; compileall |

## Known limitations

- External pre-commit hook provisioning and connected GitHub/package evidence are not reproducible in this sandbox.

## Remaining TODOs

- None for TASK-0028.

## Next deterministic step

NEXT-0028 — run the connected full-repository pre-commit gate, Python compatibility matrix, and Poetry package smoke gates.
