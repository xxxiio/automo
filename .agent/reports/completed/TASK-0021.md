---
template: task-completion-report
template_version: 2.0.0
project_owned: true
record_contract: task-completion-report
record_schema_version: 1
status: final
task_id: TASK-0021
---

# Task Completion Report

## Outcome

Completed TASK-0021: uv has been removed from every canonical developer/user and GitHub workflow. Poetry now owns development dependency installation, tests, docs, health execution, and package builds; pre-commit remains the independent source-quality gate.

## Changes made

- `.github/workflows/ci.yml`: replaced uv setup/sync/run with setup-python, Poetry installation, `poetry install`, and `poetry run`.
- `.github/workflows/publish-pypi.yml`: replaced uv build/uvx Twine with `poetry build` and `python -m twine check`.
- `pyproject.toml`: moved dev/docs tooling into Poetry dependency groups.
- `src/automo/dev/bootstrap.py`: importable PPW-style Poetry developer bootstrap.
- `scripts/init_dev.py`: thin source-checkout wrapper around the importable bootstrap.
- Public/developer documentation and active GetDone command/next-step records: Poetry-only canonical workflow.
- Synthetic examples: corrected formatting including the connected-CI E501 finding.

## Behaviour added or corrected

- AC-001: `.github/workflows/` contains no uv/setup-uv/uvx invocation.
- AC-002: current public/user/developer docs contain no uv development workflow.
- AC-003: developer bootstrap runs `poetry install --with dev` and installs/pre-provisions pre-commit.
- AC-004: GitHub tests Python 3.11-3.13 through Poetry-managed environments.
- AC-005: PyPI workflow builds with Poetry and validates with Twine.
- AC-006: bootstrap tests import installed/source-layout package code, not the non-package `scripts` directory.

## Tests added or changed

- `tests/test_init_dev.py`: tests the importable Poetry bootstrap, missing-Poetry handling, and non-Git rejection.
- `tests/test_public_release.py`: enforces Poetry-only CI/publish/developer workflows and no uv in public development docs.
- `tests/test_research_guidance_completeness.py`: propagates the source layout to its subprocess test so raw source-tree execution is deterministic.

## Validation results

- `python -m pytest -q` -> 128 passed.
- `python scripts/source_check.py` -> passed.
- `python -m compileall -q src tests scripts` -> passed.
- grep of `.github/workflows/` for uv/setup-uv/uvx -> no matches.
- grep of README/CONTRIBUTING/docs/current GetDone command records for uv workflow commands -> no matches.
- Connected pre-commit/Poetry package jobs remain required release evidence.

## Documentation updated

- `README.md`
- `CONTRIBUTING.md`
- `docs/PROJECT-SKELETON.md`
- `.agent/command-reference.md`
- `.agent/current/next-step.md`
- `.agent/current/evidence.md`
- `.agent/current/acceptance.md`

## Design decisions

- Poetry is the sole canonical project/developer/package manager for human and GitHub workflows.
- pre-commit remains independent and is executed directly in the GitHub quality job.
- uv is permitted only as an execution-environment workaround outside the canonical developer/user/GitHub contract; it is not documented in current public developer guidance.
- Historical completed GetDone records are not rewritten merely because they accurately describe prior implementation states.

## Known limitations

- This sandbox cannot provision the external Poetry/pre-commit dependencies needed to reproduce the connected package and hook jobs.

## Remaining TODOs

- Run the corrected Poetry-only candidate through connected GitHub CI and record EC-010 through EC-013 evidence.

## Risks and blockers

- Publication remains blocked until Poetry wheel/sdist smoke, Python 3.11-3.13, and the complete pre-commit gate pass in connected CI.

## Acceptance gate results

| Gate ID | Status | Evidence |
|---|---|---|
| G-001 | pass | No uv/setup-uv/uvx invocation in GitHub workflows. |
| G-002 | pass | Public/developer docs and current commands use Poetry only. |
| G-003 | pass | Importable bootstrap tests cover Poetry install and pre-commit hook installation. |
| G-004 | pass | 128 local tests, source check, and compile checks pass. |
| G-005 | not-run | Connected Poetry package builds and pre-commit environments cannot be provisioned in this sandbox. |

## Next deterministic step

NEXT-0020 — run connected GitHub CI on the corrected Poetry-only candidate and record EC-010 through EC-013 evidence.
