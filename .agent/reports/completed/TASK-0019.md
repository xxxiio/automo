---
template: task-completion-report
template_version: 2.0.0
project_owned: true
record_contract: task-completion-report
record_schema_version: 1
status: final
task_id: TASK-0019
---

# Task Completion Report

## Outcome

Completed MILESTONE-0016 consumer compatibility and project-agent composition hardening.

## Changes made

- Added GetDone-style `.project-agent/` composition with concern/path selection and changed-path inference.
- Added guidance composition locking and drift detection.
- Generalized provenance/pool/selector/calibration extension contracts.
- Added strict research-state schema checks and research-plan conformance validation.

## Behaviour added or corrected

- Project guidance is additive and can be disabled with `--no-project-agent`; it no longer needs a separate Automo-specific overlay format.
- Data-only models no longer require fake feature-set identifiers.
- Heterogeneous comparable model specs can participate in one model pool.
- Consumer selectors can remain domain-owned behind a thin Automo protocol adapter.

## Tests added or changed

- Added `tests/test_m16_compatibility.py`.
- Retained original refresh/governance/guidance regression coverage.
- Full Automo regression now passes 119 tests.

## Validation results

- Automo full regression: 119 passed.
- Source hygiene and compilation: passed.
- Wheel/sdist build: passed.
- xihbm compatibility subset: 19 passed.
- GetDone 1.1.2 validation recorded after the final project-record update.

## Documentation updated

- Added project-agent research-extension skill contract.
- Added consumer-plugin boundary reference.
- Updated README/getting-started guidance for project-owned extensions and composition locks.

## Design decisions

- Reuse GetDone's `.project-agent/index.json` schema shape instead of inventing an Automo-specific overlay format.
- Keep `.project-agent/` project-owned guidance separate from mutable `.automo/` state.
- Keep project guidance additive; protected Automo research safety remains canonical.

## Known limitations

- xihbm's existing contextual selector has a richer domain-specific signature and requires a thin adapter to Automo's selector protocol rather than being passed directly.
- Full xihbm suite was not executable because `polars` is unavailable locally.

## Remaining TODOs

- MILESTONE-0013 connected CI/pre-commit release evidence.

## Risks and blockers

- No M16 blocker remains.
- Public release remains blocked by M13 connected CI evidence.

## Acceptance gate results

| Gate ID | Status | Evidence |
|---|---|---|
| G-001 | pass | project-agent selection/validation/lock tests |
| G-002 | pass | provenance/pool/selector compatibility tests and refresh regression |
| G-003 | pass | state-schema and research-plan conformance tests |
| G-004 | pass | xihbm 19-test compatibility subset and project-agent smoke |
| G-005 | pass | Automo 119-test full regression |

## Next deterministic step

NEXT-0020 — run connected Python 3.11–3.13 CI and the canonical pre-commit/Ruff gate.
