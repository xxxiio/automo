project: synthetic-guidance-walkthrough
objective: minimize synthetic prediction error without sealed-OOS leakage
hypotheses:
  - id: H001
    statement: The bounded interaction feature adds material predictive information.
    diagnosis: baseline is stable but underfits a known interaction pattern
    plan:
      maximum_candidates: 2
      maximum_oos_candidates: 0
      materiality_threshold: 0.02
      intervention: add interaction feature only
    experiments:
      - id: E001
        result: validation_improvement_below_threshold
        decision: rejected
    conclusion: rejected
    project_implication: investigate whether model capacity rather than feature information is limiting
  - id: H002
    statement: A bounded nonlinear model improves the objective without temporal instability.
    diagnosis: feature information is present but the linear baseline cannot represent it
    plan:
      maximum_candidates: 2
      maximum_oos_candidates: 1
      materiality_threshold: 0.02
      intervention: replace model family only
    experiments:
      - id: E002
        result: validation_improvement_material_and_stable
        decision: shortlist
      - id: E003
        result: sealed_oos_improvement_material
        decision: accepted
    conclusion: supported
    project_implication: evaluate whether calibration is still adequate on the retained candidate
  - id: H003
    statement: Recalibration improves probability quality without reducing downstream utility.
    diagnosis: ranking is stable but calibration evidence is weak
    plan:
      maximum_candidates: 1
      maximum_oos_candidates: 1
      materiality_threshold: 0.01
      intervention: recalibration only
    experiments:
      - id: E004
        result: calibration_improves_but_downstream_utility_declines
        decision: rejected
    conclusion: rejected
    project_implication: retain the uncalibrated candidate and return the result to project planning
forbidden_actions:
  - use sealed OOS to generate a candidate
  - omit failed candidates from history
  - widen a search after observing weak validation without a new committed plan
  - replace model and feature family in one unplanned intervention
