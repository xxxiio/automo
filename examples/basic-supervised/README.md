# Basic supervised example

The smallest ordinary tabular path: CSV data source, feature engine, built-in linear runner, and numeric metric.

```bash
python examples/basic-supervised/example.py
```

Expected output is a mapping containing an `mse` value near zero.

Next: `docs/runtime.md`.
