# Structured output example

`ModelOutputBatch` can carry arbitrary immutable Python values rather than only scalar predictions.

```bash
python examples/structured-output/example.py
```

Expected output shows two structured action records.

Next: the custom-trainer example for `EvaluationContext`-based scoring.
