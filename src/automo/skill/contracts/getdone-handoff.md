# GetDone handoff contract

Automo owns research state and never writes `.agent/`. GetDone owns development state and never decides or rewrites `.automo/` research outcomes. The handoff contains capability ID, reason, required inputs/outputs, acceptance criteria, allowed/forbidden paths, and the requesting program/hypothesis/experiment provenance. Returned implementation evidence is independently checked by Automo before research resumes.
