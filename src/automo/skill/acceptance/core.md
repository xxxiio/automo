# Core research acceptance

Pass only when an active hypothesis provides the scientific context, the experiment is reproducible, budgets and evidence boundaries are committed, required capability/data checks pass, and outputs are persisted with research provenance. Fail or block if sealed OOS was used during search, an undeclared intervention occurred, evidence was rewritten, or project-management milestones are treated as scientific evidence.
