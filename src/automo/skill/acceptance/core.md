# Core research acceptance gate

Pass only when the active milestone is approved/active, the experiment is reproducible, budgets and evidence boundaries are committed, required capability/data checks pass, and outputs are persisted. Fail or block if sealed OOS was used during search, an undeclared intervention occurred, or evidence was rewritten.
