# Feature research workflow

Change only declared feature groups or bounded feature additions/removals. Hold model, split, objective, and evaluation policy fixed unless the milestone explicitly studies their interaction. Require an ablation or comparable control before attributing an effect to a feature group. Never synthesize arbitrary unbounded feature combinations.
