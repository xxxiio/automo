# Model comparison standard

Compare models only on compatible objectives, metric definitions/directions, data partitions, and evidence scope. Distinguish statistical improvement from practical minimum effect. Report stability and observation counts alongside point estimates. Do not rank incomparable metrics by arbitrary normalization unless that policy was committed beforehand.
