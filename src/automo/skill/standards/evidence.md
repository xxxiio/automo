# Evidence standard

Persist immutable identities for datasets, models, calibrations, candidates, splits, and evaluation artifacts. Report attempted variants and exposure counts. Separate validation, sealed research OOS, and refresh-OOS evidence. Claims must cite persisted evidence; unavailable checks are reported as unavailable, not passed.
