# Leakage standard

No candidate generation, hyperparameter choice, feature choice, graph choice, or calibration choice may use sealed OOS outcomes. Meta-model fitting uses cross-fitted upstream outputs on fit data. Any accidental OOS exposure invalidates the affected research boundary and must be recorded.
