# Capability escalation policy

Missing implementation capability is a blocker, not permission to mutate production code inside a research iteration. Persist a scoped capability request, optionally delegate through GetDone, protect prior research evidence, independently verify changed files/evidence, then resume only if the required capability contract is satisfied.
